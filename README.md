# Dirty-fast-cheap-RTSP-view-server
# a quick and dirty way to view two RTSP streams from cameras in a modern browser. With Node.js
let's assume we already have Node.js installed, two running RTSP camera
open cmd
run: node server.js
run: npx http-server
open: index.html
